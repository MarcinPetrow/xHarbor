# xbacklog-web

Node web app for `xBacklog`, intended to sit in front of `xbacklog-api`.

Run:

```bash
npm run dev:xbacklog
npm run dev:xbacklog-web
```

Then open:

```text
http://127.0.0.1:3001
```
